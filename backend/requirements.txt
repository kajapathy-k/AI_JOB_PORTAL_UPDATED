# Web framework (all services + gateway)
fastapi
uvicorn[standard]
python-multipart          # file/form uploads (resume PDF, interview audio)
pydantic[email]           # EmailStr validation in auth-service

# Auth / security
python-jose[cryptography] # JWT mint/verify
bcrypt                    # password hashing (used directly; see common/security.py)

# Persistence
SQLAlchemy
psycopg[binary]

# AI / orchestration
groq                      # STT (Whisper) + LLM (Llama) + TTS (Orpheus)
langgraph                 # interview loop + resume-screening orchestration
python-dotenv

# Gateway + inter-service calls
httpx

# Resume parsing
pypdf                     # extract text from uploaded PDF resumes

# Security constraints for transitive packages flagged by container scans.
jaraco.context>=6.1.0
langsmith>=0.8.18
setuptools>=80.0.0
wheel>=0.46.2
