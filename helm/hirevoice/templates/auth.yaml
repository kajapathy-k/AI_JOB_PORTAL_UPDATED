apiVersion: apps/v1
kind: Deployment
metadata:
  name: hirevoice-auth
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "hirevoice.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth
spec:
  replicas: {{ .Values.services.auth.replicas }}
  selector:
    matchLabels:
      {{- include "hirevoice.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: auth
  template:
    metadata:
      labels:
        {{- include "hirevoice.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: auth
    spec:
      {{- include "hirevoice.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: auth
          image: {{ include "hirevoice.backendImage" . }}
          imagePullPolicy: {{ .Values.images.backend.pullPolicy }}
          command: ["python"]
          args: ["-m", "uvicorn", "services.auth.main:app", "--host", "0.0.0.0", "--port", "{{ .Values.services.auth.port }}"]
          ports:
            - name: http
              containerPort: {{ .Values.services.auth.port }}
          {{- include "hirevoice.backendConfigEnv" . | nindent 10 }}
          {{- include "hirevoice.rdsEnv" . | nindent 10 }}
          {{- include "hirevoice.jwtEnv" . | nindent 12 }}
          {{- include "hirevoice.httpProbes" . | nindent 10 }}
          resources:
            {{- toYaml .Values.services.auth.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: hirevoice-auth
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "hirevoice.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth
spec:
  type: ClusterIP
  selector:
    {{- include "hirevoice.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: auth
  ports:
    - name: http
      port: {{ .Values.services.auth.port }}
      targetPort: http
