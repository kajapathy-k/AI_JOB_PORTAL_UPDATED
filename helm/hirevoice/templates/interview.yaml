apiVersion: apps/v1
kind: Deployment
metadata:
  name: hirevoice-interview
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "hirevoice.labels" . | nindent 4 }}
    app.kubernetes.io/component: interview
spec:
  replicas: {{ .Values.services.interview.replicas }}
  selector:
    matchLabels:
      {{- include "hirevoice.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: interview
  template:
    metadata:
      labels:
        {{- include "hirevoice.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: interview
    spec:
      {{- include "hirevoice.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: interview
          image: {{ include "hirevoice.backendImage" . }}
          imagePullPolicy: {{ .Values.images.backend.pullPolicy }}
          command: ["python"]
          args: ["-m", "uvicorn", "services.interview.main:app", "--host", "0.0.0.0", "--port", "{{ .Values.services.interview.port }}"]
          ports:
            - name: http
              containerPort: {{ .Values.services.interview.port }}
          {{- include "hirevoice.backendConfigEnv" . | nindent 10 }}
          env:
            {{- include "hirevoice.jwtEnv" . | nindent 12 }}
            {{- include "hirevoice.groqEnv" . | nindent 12 }}
          {{- include "hirevoice.httpProbes" . | nindent 10 }}
          resources:
            {{- toYaml .Values.services.interview.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: hirevoice-interview
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "hirevoice.labels" . | nindent 4 }}
    app.kubernetes.io/component: interview
spec:
  type: ClusterIP
  selector:
    {{- include "hirevoice.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: interview
  ports:
    - name: http
      port: {{ .Values.services.interview.port }}
      targetPort: http
